<% provide(:title, 'All users') %>
<div class = "row">
	<aside class = "span9 offset3">
		<h1>All users</h1>

		<%= will_paginate %>
		<div class = "row">
		<ul class="users">
			<%= render @users %>
		</ul>
		</div>

		<%= will_paginate %>
	</aside>
</div>